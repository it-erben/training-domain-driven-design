spring:
  application:
    name: immobilien-crm
  datasource:
    url: jdbc:h2:mem:realestate
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    problemdetail:
      enabled: true
  threads:
    virtual:
      enabled: true

logging:
  structured:
    format:
      console: ecs

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8080
