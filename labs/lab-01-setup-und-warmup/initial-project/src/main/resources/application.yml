spring:
  application:
    name: foerderantragsverwaltung
  datasource:
    url: jdbc:h2:mem:foerderung
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    problemdetails:
      enabled: true
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8080
